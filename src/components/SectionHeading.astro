---
import { withBase } from "@/utils/url";

interface Props {
	href?: string;
	label: string;
}

const { href, label } = Astro.props;
---

<div class="section-heading">
	<h2>{label}</h2>
	{href && <a href={withBase(href)}>View all <span aria-hidden="true">→</span></a>}
</div>
