# Content Guide

Add public content to the collection directories and copy frontmatter from
`_templates/`.

File names become URL slugs, so prefer short lowercase names separated by
hyphens. Keep work-in-progress entries marked `draft: true`.

Topic names are normalized to lowercase. Prefer the initial vocabulary:
`gpu`, `cuda`, `ai-systems`, `hardware`, `rust`, and `linux`.
